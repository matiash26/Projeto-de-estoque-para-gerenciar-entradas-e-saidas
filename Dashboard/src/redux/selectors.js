export function selectAlert(rootReducer) {
  return rootReducer.alertReducer;
}
export function selectService(rootReducer) {
  return rootReducer.serviceReducer;
}
export function selectModals(rootReducer) {
  return rootReducer.modalsReducer;
}
export function selectProducts(rootReducer) {
  return rootReducer.productReducer;
}
export function selectStock(rootReducer) {
  return rootReducer.stockReducer;
}
export function selectEntries(rootReducer) {
  return rootReducer.entriesReducer;
}
export function selectUser(rootReducer) {
  return rootReducer.userReducer;
}
